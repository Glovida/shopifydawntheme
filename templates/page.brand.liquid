{% assign linklist = linklists['brands'] %}

<ul>
{% for link in linklists.brands.links %}
  <li>
  {% if {% link.title | slice 0 %} == "A" %}
    <a href="{{ link.url }}">{{ link.title }}</a>
  {% endif %}
  </li>
{% endfor %}
</ul>